<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RentBookRentPropertyManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorSecondaryContainer">#C4E4E2</item>

    </style>

    <style name="Theme.RentBookRentPropertyManager.Login" parent="Theme.Material3.DayNight.NoActionBar" >

    </style>

    <style name="Theme.RentBookRentPropertyManager.ToolBar" parent="Theme.MaterialComponents.DayNight.DarkActionBar" >

        <!-- Toolbar background color -->
        <item name="colorPrimary">@color/primary_main</item>
        <item name="android:windowLightStatusBar">false</item>

    </style>

    <style name="CropperTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:windowFullscreen">false</item>
        <item name="android:fitsSystemWindows">true</item>
        <!-- Toolbar background color -->
        <item name="colorPrimary">@color/primary_main</item>

    </style>
</resources>